<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appsofawesome.keyboardutilities"
    android:versionCode="2"
    android:versionName="1.1.1" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data android:value="true" android:name="xposedmodule"/>
        <meta-data android:value="30+" android:name="xposedminversion"/>
        <meta-data android:value="Kills the alt-tab consumption.\nAllows applications to handle it instead." android:name="xposeddescription"/>
    </application>

</manifest>
